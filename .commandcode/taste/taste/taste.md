# Taste
- User works on WordPress plugin development (PHP backend + block editor JS/CSS), local dev via Studio/WP-CLI. Confidence: 0.9
- User writes in Italian — respond in Italian. Confidence: 0.9
- User cross-checks conclusions with a second AI ("È vero?"), often pasting the other AI's full analysis (e.g. Claude Opus) and asking it to be explained: verify every claim against the actual code first, then explain — label findings correct / partially correct, never overclaim certainty. Confidence: 0.7
- When asked to analyze/audit code, work read-only: do not modify any file unless explicitly asked. Confidence: 0.9
- Very conservative audit criterion (stated repeatedly): never classify code as unused or over-engineered if it could help stability, compatibility, or edge-case handling even in ~10% of cases; when in doubt, report it as "element to verify" rather than a certain problem. Confidence: 0.95
- Before declaring code "dead", verify it isn't public API surface (public methods, filters/hooks exposed to third parties) — no internal callers ≠ dead code. Confidence: 0.7
- Audit report format: for each finding give file + affected line numbers, motivation, impact, and possible simplification. Confidence: 0.9
- Reuse the host platform's native APIs, components, markup and styles (WordPress core) instead of custom implementations, to stay coherent with the core. Confidence: 0.9
- Treat deprecated public methods as compatibility shims: keep them through the current major series, document the removal, and delete only at the next major version with a migration note. Confidence: 0.6
- When asked to save a report, persist it to a markdown .md file exactly/verbatim as it appears in chat. Confidence: 0.7
- Verify changes before reporting done: syntax lint (`php -l`, `node --check`) and PHP smoke tests via WP-CLI/Studio (`studio wp eval-file`). Confidence: 0.7
- A double-check/quality request means a structured second verification pass beyond re-running the tests: verify hook wiring (e.g. sanitize actually hooked to admin_init), side effects on related logic (e.g. extended type list feeding schema ownership), edge cases, and a real end-to-end render of the affected page (wp-cli as admin, inspecting the actual HTML). Confidence: 0.7
- When a recommendation introduces unfamiliar acronyms/concepts, the user asks for a plain explanation of what each is for and wants bundled terms split into their distinct parts ("LocalBusiness/NAP/geo/GBP — sembrano più cose"). Explain each concept's practical purpose instead of assuming jargon familiarity. Confidence: 0.7
- In admin UI reviews, user scrutinizes visual/ergonomic consistency of form fields (standard padding/dimensions matching WP admin defaults), not just functional correctness — flag inconsistencies with concrete fixes. Confidence: 0.7
- User is skeptical of flat "mega list" selects (e.g., Business type): wants evaluation of whether the presentation is efficient — a long list can be simultaneously too much in some contexts and too little in others; prefer discussing alternatives (grouping, search/filter, hierarchical or adaptive options) over defending the flat list. Confidence: 0.6
- When explaining proposed fixes, the user wants them "in modo pragmatico/concreto": what each fix means in practice, concrete steps, risk level ("zero rischio"/"basso rischio") — not abstract theory. Confidence: 0.9
- After a verified analysis, a terse approval like "Procedi all'implementazione" means full delegation: implement end-to-end autonomously (code + tests + verification), decide open design points yourself instead of re-asking option questions, and close with a concrete summary of what was decided and what was rejected and why. Confidence: 0.6
- When changing behavior in the WordPress project, extend the existing smoke tests (tests/*.php, standalone scripts run via `studio wp eval-file`; some need `--skip-plugins=easyrankly`) with assertions covering the new behavior — not just manual checks. Confidence: 0.6
- Refactors must preserve the contract of publicly exposed WordPress filters: keep the original data shape as the single source for validation/filtering and add a separate method for new rendering needs instead of changing the filtered structure. Confidence: 0.6
 not code. Confidence: 0.6
- When planning implementation work, draft the plan as a self-contained prompt ("snippet") for handoff to an external AI executor (GLM 5.3 Flash): the assistant writes the executable prompt, the user runs it there, and the assistant then reviews the produced code. Confidence: 0.8
- User maintains numbered report files (e.g. report3.md, report3-5.md) as persistent project state — read them to reconstruct context before status analyses. Confidence: 0.7
- User's quality bar for the plugin is perfection ("un'opera d'arte"): analyze exhaustively what remains to be done and explicitly verify regressions/new errors introduced by recent modifications. Confidence: 0.8
- EasyRankly house style is deliberate and non-negotiable: tabs for indentation, WordPress Coding Standards, Yoda conditions (`'noindex' === $value`), aligned `=>` inside array literals, alphabetically ordered array keys where the surrounding code already does that, docblocks only on public methods (private helpers deliberately undocumented), and no new return types — match `: void`/`: bool`/`: string`/`: array` only where already present. Confidence: 0.9
- EasyRankly hard architectural constraints: single `final class ERankly_Plugin` composed of traits (one file per responsibility in `includes/`), all state static, every hook registered in `ERankly_Plugin::init()`, no global functions (the legacy `erankly_get_business_profile()` shim is the only exception), and never add a public filter surface — `apply_filters( 'erankly_...' )` is forbidden and a grep smoke test fails the build on it. Confidence: 0.9
- Persistence-compatibility boundary in EasyRankly: never rename or remove existing constants, methods, hooks, meta keys or options; reuse `erankly_visibility` / `erankly_meta_description` instead of inventing new key names, and reuse the existing sanitizer instead of adding a second one. Confidence: 0.9
- Output discipline in EasyRankly: every user-facing string via `__()` / `esc_html_e()` with text domain `easyrankly`; escape all output (`esc_attr`, `esc_html`, `esc_textarea`); sanitize every `$_POST` read with `wp_unslash()` + a sanitizer, and guard save hooks with nonce + capability checks so REST/CLI/importer paths can never clobber a stored value. Confidence: 0.9
- Never invent content: the plugin only prints text a human typed or text WordPress owns — no generated summaries, no truncated post content. Confidence: 0.9
- Smoke tests have an exact fixed shape: self-contained script, guard `ABSPATH` + `class_exists( 'ERankly_Plugin' )`, `require __DIR__ . '/bootstrap.php'`, create every fixture, wrap assertions in `try { … } finally { … }`, delete every fixture, restore `$GLOBALS['wp_query']`/`$GLOBALS['post']` and any filter, use the `$assert` and `$reset_request_caches` bootstrap helpers, never depend on existing site content, end with `echo "… passed.\n";`. Confidence: 0.9
- Release metadata discipline: version bumped in all four places (plugin header `Version:`, `private const VERSION`, readme `Stable tag:`, block.json `"version"`) because a smoke test asserts they are identical; changelog = one line per user-visible change in imperative past tense; upgrade notice = two short lines; never add a `== Testing ==` section or touch `Domain Path` (both forbidden by smoke test). Confidence: 0.9
- Minimal churn when implementing: do not reformat files not being changed, do not add dependencies, do not create a `languages/` directory. Confidence: 0.8
- Implementation report format (after completing a brief): files changed, diff summary, full test output, and anything decided that the brief did not cover. Confidence: 0.8
